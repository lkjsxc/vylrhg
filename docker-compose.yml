services:
  vylrhg:
    build: .
    image: vylrhg:local
    stdin_open: true
    tty: true
    working_dir: /work
    volumes:
      - ./:/work
    # Example: open ./README.md inside the container
    # command: ["/work/README.md"]
