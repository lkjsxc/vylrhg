# Conventions

- Module IDs are lowercase, hyphenated, and globally unique.
- Handler names are namespaced by module ID.
- Type names are PascalCase; values are snake_case.
- No implicit defaults; all required fields are explicit.
