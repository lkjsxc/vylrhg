# Markup Model

- Document root defines module scope.
- Nodes are typed and immutable.
- Sidecar nodes attach handlers or metadata to primary nodes.
- Node IDs are stable across parse and render stages.
