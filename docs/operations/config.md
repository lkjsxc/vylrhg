# Configuration

- Configuration is a typed manifest file.
- Values are namespaced by subsystem.
- No dynamic overrides; all changes require reload.
- DATA_DIR sets the persistent data path (default: /data).
