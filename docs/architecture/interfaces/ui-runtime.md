# UI to Runtime Interface

- Input events are normalized into typed commands.
- UI state changes are expressed as declarative messages.
- Runtime returns acknowledgements and effect completions.
- No direct UI mutation from runtime; all UI changes are via events.
