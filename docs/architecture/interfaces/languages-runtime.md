# Languages to Runtime Interface

- Markup and assembly modules bind via shared module IDs.
- Assembly handlers are referenced by sidecar nodes.
- VM loads modules deterministically by dependency order.
- Type checking is performed before module activation.
