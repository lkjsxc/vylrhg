# Dependencies

- Renderer depends on markup parser and layout engine.
- VM depends on assembly compiler and effect dispatcher.
- UI depends on tab manager and layout engine.
- Persistence depends on schema stability.
