# Milestones

## Milestone Set

- M1: core runtime + event bus + persistence shell.
- M2: markup parser + renderer pipeline + layout integration.
- M3: assembly compiler + VM + effect system.
- M4: tab manager + tiling engine + UI input layer.
- M5: end-to-end integration + stability hardening.
