# Tabs

## Identity

- Tab identity is stable across sessions.
- Tab IDs are used as VM isolation boundaries.

## Operations

- Tabs can be pinned, detached, and duplicated.
- Tab groups map to independent layout trees.
- Tab lifecycle is persisted in session store.
